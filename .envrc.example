# Example environment variables for the Markdown Link Crawler

# Optional: Required if using a non-local/authenticated Firecrawl instance
# export FIRECRAWL_API_KEY=

# Optional: Defaults to Firecrawl's default if not set. Example shows local instance.
export FIRECRAWL_API_URL="http://localhost:3002"

# Required: URL for your Supabase project's Postgrest endpoint.
export SUPABASE_URL="http://localhost:54321"

# Required: The service role key for your Supabase project.
export SUPABASE_SERVICE_ROLE_KEY="YOUR_SERVICE_ROLE_KEY"

# Required: The anonymous public key for your Supabase project (needed for frontend).
export SUPABASE_ANON_KEY="YOUR_ANON_KEY"

# Required for summarization feature using OpenAI.
export OPENAI_API_KEY="YOUR_OPENAI_API_KEY"

# Optional: Defaults to gpt-4o-mini
# export OPENAI_MODEL="gpt-4.1-nano"
